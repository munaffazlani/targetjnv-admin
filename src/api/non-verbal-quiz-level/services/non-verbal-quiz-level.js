'use strict';

/**
 * non-verbal-quiz-level service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::non-verbal-quiz-level.non-verbal-quiz-level');
