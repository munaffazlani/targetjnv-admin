'use strict';

/**
 * non-verbal-quiz-level controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::non-verbal-quiz-level.non-verbal-quiz-level');
