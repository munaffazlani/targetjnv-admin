'use strict';

/**
 * non-verbal-quiz-level router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::non-verbal-quiz-level.non-verbal-quiz-level');
